<section id="vitamins-products" class="sports-nutrition-section">
  <div class="sports-container">

    <!-- LEFT CONTENT -->
    <div class="left-content reveal-left">
      <h2 class="section-heading">Biobolics Vitamins</h2>

      <p class="description">
        Our Vitamins range is developed to support daily wellness with precision, purity, and science-backed formulations. Each product is crafted to help maintain balance, strength, and overall vitality.
      </p>

      <!-- ICONS WITH TOOLTIPS -->
      <div class="icon-row">
        <div class="icon-item">
          <img src="/icons/vitamins/heart-health.svg" alt="Heart Health" />
          <span class="icon-tooltip">Heart Health</span>
        </div>

        <div class="icon-item">
          <img src="/icons/vitamins/bone-health.svg" alt="Bone Health" />
          <span class="icon-tooltip">Bone Health</span>
        </div>

        <div class="icon-item">
          <img src="/icons/vitamins/brain-health.svg" alt="Brain Health" />
          <span class="icon-tooltip">Brain Health</span>
        </div>

        <div class="icon-item">
          <img src="/icons/vitamins/probobiotics.svg" alt="Probiotics" />
          <span class="icon-tooltip">Probiotics Support</span>
        </div>

        <div class="icon-item">
          <img src="/icons/vitamins/fish-oil.svg" alt="Fish Oil" />
          <span class="icon-tooltip">Fish Oil Benefits</span>
        </div>
      </div>

      <!-- BUTTON -->
      <div class="cta-wrapper">
        <span class="cta-button">LEARN MORE</span>
      </div>
    </div>

    <!-- RIGHT IMAGE -->
    <div class="right-image reveal-right">
      <img src="/images/vitamins/vitamins-main.webp" alt="Vitamins Main" />
    </div>

  </div>
</section>

<script>
if (typeof window !== 'undefined') {
  const observer = new IntersectionObserver(
    entries => {
      entries.forEach(entry => {
        entry.target.classList.toggle('is-visible', entry.isIntersecting);
      });
    },
    { threshold: 0.15 }
  );

  document
    .querySelectorAll('.reveal-left, .reveal-right')
    .forEach(el => observer.observe(el));
}
</script>

<style>
/* ===============================
   AUTO COLOR (DARK MODE SAFE)
================================ */
.sports-nutrition-section {
  --text-main: #ffffff;
  --text-muted: rgba(255,255,255,0.88);
  --tooltip-bg: rgba(10,15,42,0.95);
}

/* ===============================
   LAYOUT + BACKGROUND
================================ */
.sports-nutrition-section {
  width: 100%;
  padding: 6rem 0;
  background-image: url('/images/backgrounds/vitamins-bg.webp');
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  color: var(--text-main);
}

.sports-container {
  display: flex;
  align-items: stretch;
  width: 100%;
}

/* ===============================
   LEFT
================================ */
.left-content {
  flex: 1;
  padding: 3rem 2.5rem 3rem 125px;
  display: flex;
  flex-direction: column;
}

/* ===============================
   RIGHT
================================ */
.right-image {
  flex: 1;
  padding: 3rem 125px 3rem 2.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
}

.right-image img {
  max-width: 100%;
  height: auto;
  display: block;
}

/* ===============================
   TYPOGRAPHY
================================ */
.section-heading {
  font-size: 3.64rem;
  font-weight: 700;
  margin-bottom: 1.5rem;
}

.description {
  font-size: 1.3rem;
  line-height: 1.7;
  color: var(--text-muted);
  margin-bottom: 2.2rem;
}

/* ===============================
   ICONS
================================ */
.icon-row {
  display: flex;
  gap: 2.8rem;
  margin-bottom: 2.5rem;
}

.icon-item {
  position: relative;
}

.icon-item img {
  width: 72px;
  height: 72px;
  transition: transform 0.35s ease, filter 0.35s ease;
}

.icon-item:hover img {
  filter: invert(22%) sepia(92%) saturate(3200%) hue-rotate(10deg);
  transform: translateY(-3px);
}

.icon-tooltip {
  position: absolute;
  bottom: -38px;
  padding: 6px 12px;
  font-size: 0.9rem;
  font-weight: 600;
  background: var(--tooltip-bg);
  color: #ffffff;
  border-radius: 8px;
  opacity: 0;
  transform: translateY(6px);
  transition: all 0.3s ease;
}

.icon-item:hover .icon-tooltip {
  opacity: 1;
  transform: translateY(0);
}

/* ===============================
   PREMIUM CTA (UNCHANGED)
================================ */
.cta-wrapper {
  margin-top: auto;
}

.cta-button {
  padding: 0.75rem 2.4rem;
  font-size: 1.05rem;
  font-family: 'Montserrat', system-ui, sans-serif;
  font-weight: 500;
  letter-spacing: 0.14em;
  color: #0a0f2a;
  background: linear-gradient(145deg, #f2f2f2, #d6d6d6);
  border-radius: 999px;
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.9),
    0 12px 30px rgba(0,0,0,0.35);
  transition: all 0.4s ease;
}

.cta-button:hover {
  background: linear-gradient(145deg, #ff2b2b, #c90000);
  color: #ffffff;
  transform: translateY(-2px);
}

/* ===============================
   REVEAL
================================ */
.reveal-left {
  opacity: 0;
  transform: translateX(-60px);
  transition: all 0.8s cubic-bezier(0.22,1,0.36,1);
}

.reveal-right {
  opacity: 0;
  transform: translateX(60px);
  transition: all 0.8s cubic-bezier(0.22,1,0.36,1);
}

.is-visible {
  opacity: 1;
  transform: translateX(0);
}

/* ===============================
   MOBILE
================================ */
@media (max-width: 768px) {
  .sports-container {
    flex-direction: column-reverse;
    padding: 0 2rem;
  }

  .left-content,
  .right-image {
    padding: 2rem 1.5rem;
  }

  .section-heading {
    font-size: 2.2rem;
  }

  .description {
    font-size: 1.2rem;
  }
}
</style>

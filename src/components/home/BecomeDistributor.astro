<section id="distributor-section" class="distributor-section">
  <div class="distributor-content">
    <h2 class="section-heading">Worldwide Distribution Inquiries.</h2>

    <p class="description">
      We are expanding our distribution network and are looking for distributors Worldwide. If you wish to become a Biobolics distributor in your country, please email us at: <strong>international@biobolics.com</strong>
    </p>

    <p class="description">
      Want to Know More? <br />
      If you want to know more about our products, drop us an email with your interests, we'll be happy to help.
    </p>

    <div class="cta-wrapper">
      <button class="cta-button">Apply Now</button>
    </div>
  </div>

  <style>
    .distributor-section {
      width: 100%;
      padding: 6rem 2rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      position: relative;
      text-align: center;
      overflow: hidden;
      background: linear-gradient(to bottom, #0a0f2a, #1a3b8f); /* Dark → Light Blue */
    }

    /* Full-width background map with dark overlay */
    .distributor-section::before {
      content: "";
      position: absolute;
      top: 50%;
      left: 50%;
      width: 100%;
      height: 100%;
      transform: translate(-50%, -50%);
      background: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4)),
                  url("/images/distributor/world-map-dots.webp") no-repeat center center;
      background-size: cover; /* Fill full width, responsive */
      opacity: 0.5; /* Map slightly darker */
      pointer-events: none;
      z-index: 0;
    }

    .distributor-content {
      max-width: 900px;
      position: relative; /* Text above background */
      z-index: 1;
    }

    .section-heading {
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 1.5rem;
      color: #ffffff;
    }

    .description {
      font-size: 1.2rem;
      line-height: 1.7;
      margin-bottom: 1.5rem;
      color: #e0e0e0;
    }

    .cta-wrapper {
      margin-top: 1.5rem;
    }

    .cta-button {
      padding: 12px 28px;
      font-size: 1.1rem;
      font-weight: 600;
      border-radius: 12px;
      border: none;
      cursor: pointer;
      background: linear-gradient(to bottom, #ff3b3b, #d80000);
      color: #ffffff;
      transition: all 0.35s ease;
    }

    .cta-button:hover {
      background: linear-gradient(to bottom, #d0d0d0, #a9a9a9);
      color: #0a0f2a;
    }

    @media (max-width: 768px) {
      .section-heading {
        font-size: 2rem;
      }

      .description {
        font-size: 1rem;
      }
    }
  </style>
</section>

<section id="biobolics-gummies" class="sports-nutrition-section">
  <div class="sports-container">

    <!-- LEFT IMAGE -->
    <div class="left-image reveal-left">
      <img
        src="/images/gummies/gummies-main.webp"
        alt="Biobolics Gummies"
      />
    </div>

    <!-- RIGHT CONTENT -->
    <div class="right-content reveal-right">
      <h2 class="section-heading">
        Biobolics Gummies
      </h2>

      <p class="description">
        Biobolics Gummies are formulated for convenient, tasty, and effective daily nutrition. Each gummy contains scientifically selected nutrients to support wellness, energy, and immunity for all ages.
      </p>

      <!-- ICONS WITH TOOLTIPS -->
      <div class="icon-row">
        <div class="icon-item">
          <img src="/icons/gummies/vitamin-c.svg" alt="Vitamin C" />
          <span class="icon-tooltip">Vitamin C</span>
        </div>

        <div class="icon-item">
          <img src="/icons/gummies/immune.svg" alt="Immunity" />
          <span class="icon-tooltip">Immunity</span>
        </div>

        <div class="icon-item">
          <img src="/icons/gummies/energy.svg" alt="Energy" />
          <span class="icon-tooltip">Energy</span>
        </div>

        <div class="icon-item">
          <img src="/icons/gummies/brain.svg" alt="Brain Support" />
          <span class="icon-tooltip">Brain Support</span>
        </div>

        <div class="icon-item">
          <img src="/icons/gummies/digestion.svg" alt="Digestion" />
          <span class="icon-tooltip">Digestion</span>
        </div>
      </div>

      <!-- BUTTON -->
      <div class="cta-wrapper">
        <button class="cta-button">Visit Now</button>
      </div>
    </div>

  </div>
</section>

<script>
if (typeof window !== 'undefined') {
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        entry.target.classList.toggle('is-visible', entry.isIntersecting);
      });
    },
    { threshold: 0.15 }
  );

  document
    .querySelectorAll('.reveal-left, .reveal-right')
    .forEach(el => observer.observe(el));
}
</script>

<style>
/* ===============================
   SECTION + BACKGROUND IMAGE
================================ */
.sports-nutrition-section {
  width: 100%;
  padding: 6rem 0;

  /* 🔥 BACKGROUND IMAGE HERE */
  background-image: url('/images/gummies/gummies-bg.webp');
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

/* ===============================
   LAYOUT
================================ */
.sports-container {
  display: flex;
  align-items: stretch;
  width: 100%;
}

/* LEFT */
.left-image {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
}

.left-image img {
  max-width: 100%;
  height: auto;
}

/* RIGHT */
.right-content {
  flex: 1;
  padding: 3rem 125px 3rem 2.5rem;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

/* ===============================
   TYPOGRAPHY
================================ */
.section-heading {
  font-size: 3.64rem;
  font-weight: 700;
  margin-bottom: 1.5rem;
}

.description {
  font-size: 1.3rem;
  line-height: 1.7;
  text-align: justify;
  margin-bottom: 2.2rem;
}

/* ===============================
   ICONS + TOOLTIPS
================================ */
.icon-row {
  display: flex;
  gap: 2.8rem;
  margin-bottom: 2.5rem;
}

.icon-item {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

@media (min-width: 769px) {
  .icon-item img {
    width: 72px;
    height: 72px;
  }
}

.icon-item img {
  transition: filter 0.35s ease, transform 0.35s ease;
  cursor: pointer;
}

.icon-tooltip {
  position: absolute;
  bottom: -38px;
  padding: 6px 12px;
  font-size: 0.9rem;
  font-weight: 600;
  background: rgba(10, 15, 42, 0.9);
  color: #ffffff;
  border-radius: 8px;
  white-space: nowrap;
  opacity: 0;
  transform: translateY(6px);
  pointer-events: none;
  transition: opacity 0.3s ease, transform 0.3s ease;
}

.icon-item:hover .icon-tooltip {
  opacity: 1;
  transform: translateY(0);
}

.icon-item:hover img {
  filter: invert(22%) sepia(92%) saturate(3200%) hue-rotate(10deg);
  transform: translateY(-3px);
}

/* ===============================
   BUTTON
================================ */
.cta-wrapper {
  margin-top: auto;
}

.cta-button {
  padding: 12px 28px;
  font-size: 1.1rem;
  font-weight: 600;
  border-radius: 12px;
  border: none;
  cursor: pointer;
  background: linear-gradient(to bottom, #ff3b3b, #d80000);
  color: #ffffff;
  transition: all 0.35s ease;
}

.cta-button:hover {
  background: linear-gradient(to bottom, #d0d0d0, #a9a9a9);
  color: #0a0f2a;
}

/* ===============================
   REVEAL
================================ */
.reveal-left {
  opacity: 0;
  transform: translateX(-60px);
  transition: all 0.8s cubic-bezier(0.22,1,0.36,1);
}

.reveal-right {
  opacity: 0;
  transform: translateX(60px);
  transition: all 0.8s cubic-bezier(0.22,1,0.36,1);
}

.reveal-left.is-visible,
.reveal-right.is-visible {
  opacity: 1;
  transform: translateX(0);
}

/* ===============================
   MOBILE
================================ */
@media (max-width: 768px) {
  .sports-container {
    flex-direction: column;
  }

  .right-content {
    padding: 2rem;
  }

  .section-heading {
    font-size: 2.2rem;
  }

  .description {
    font-size: 1.2rem;
  }

  .icon-row {
    justify-content: space-between;
    gap: 1.4rem;
  }
}
</style>

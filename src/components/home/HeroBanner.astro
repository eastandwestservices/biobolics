<!-- ===============================
     HERO SECTION
================================= -->
<section class="hero-banner">
  <!-- Canvas for particles + blobs -->
  <canvas id="heroParticles" class="hero-bg" aria-hidden="true"></canvas>

  <div class="hero-container">
    <div class="hero-center">
      <!-- Hero slogan -->
      <h1>SCIENCE AND POWER OF NUTRITION</h1>

      <!-- 3-line description -->
      <p class="hero-description">
        Biobolics delivers a full range of Premium Nutritions for all ages.<br />
        Pre-Workout, Post-Workout, Vitamins & Minerals,<br />
        Tasty Gummies and Stevia Products.
      </p>
    </div>
  </div>
</section>

<style>
@font-face {
  font-family: "Square721BT";
  src: url("/fonts/Square721BT-Roman.woff2") format("woff2");
  font-weight: 400;
}
@font-face {
  font-family: "Square721BT";
  src: url("/fonts/Square721BT-Bold.woff2") format("woff2");
  font-weight: 700;
}
@font-face {
  font-family: "Square721BT";
  src: url("/fonts/Square721BT-BoldExtended.woff2") format("woff2");
  font-weight: 800;
}

/* Reset & Layout */
*,
*::before,
*::after { box-sizing:border-box; margin:0; padding:0; }
html, body { width:100%; overflow-x:hidden; }
main, section, header, footer { width:100vw; margin:0; padding:0; box-sizing:border-box; }

/* Hero Banner */
.hero-banner {
  position: relative;
  width: 100%;
  height: 36vh;
  background: #0b0f14;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

/* Canvas */
.hero-bg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  background: transparent;
  z-index: 0;
}

.hero-container {
  position: relative;
  z-index: 1;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  padding: 0 2rem;
}

.hero-center {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 0.75rem;
  text-align: center;
}

/* Hero slogan */
.hero-center h1 {
  font-family: "Square721BT", sans-serif;
  font-weight: 800; 
  font-size: clamp(2.2rem, 3vw, 2.5rem);
  color: #ffffff;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

/* Hero 3-line description */
.hero-description {
  font-family: "Square721BT", sans-serif;
  font-weight: 400; /* not bold */
  font-size: clamp(1.86rem, 2.54vw, 2.125rem);
  color: #ffffff;
  opacity: 0.85;
  margin-top: 0.5rem;
  line-height: 1.4;
  text-align: center;
}

/* Responsive */
@media (max-width: 1024px) {
  .hero-banner { height: 40vh; }
  .hero-center h1 { font-size: 1.8rem; }
  .hero-description { font-size: 1.86rem; }
}
@media (max-width: 768px) {
  .hero-banner { height: 45vh; }
  .hero-center h1 { font-size: 1.5rem; }
  .hero-description { font-size: 1.6rem; }
}
@media (max-width: 480px) {
  .hero-banner { height: 50vh; }
  .hero-center h1 { font-size: 1.2rem; }
  .hero-description { font-size: 1.35rem; }
}
</style>

<script type="module">
// Particle/Blob animation code
const canvas = document.getElementById("heroParticles");
const ctx = canvas.getContext("2d");

let particles = [];
let blobs = [];
let particleCount = window.innerWidth > 768 ? 60 : 20;
let blobCount = window.innerWidth > 768 ? 7 : 2;
let mouse = { x: null, y: null };

function resizeCanvas() {
  const hero = document.querySelector('.hero-banner');
  canvas.width = window.innerWidth;
  canvas.height = hero.offsetHeight;
}
window.addEventListener("resize", () => {
  particleCount = window.innerWidth > 768 ? 60 : 20;
  blobCount = window.innerWidth > 768 ? 7 : 2;
  resizeCanvas();
});
resizeCanvas();

window.addEventListener("mousemove", (e) => { mouse.x = e.clientX; mouse.y = e.clientY; });

function drawHexagon(x, y, radius, angle, color){
  ctx.beginPath();
  for(let i=0;i<6;i++){
    const a = (Math.PI/3)*i + angle;
    const px = x + radius * Math.cos(a);
    const py = y + radius * Math.sin(a);
    if(i===0) ctx.moveTo(px, py); else ctx.lineTo(px, py);
  }
  ctx.closePath();
  ctx.fillStyle=color;
  ctx.fill();
}

class Particle {
  constructor(){this.reset();}
  reset(){
    this.x=Math.random()*canvas.width;
    this.y=Math.random()*canvas.height;
    this.radius=3+Math.random()*3;
    this.speedX=-0.3+Math.random()*0.6;
    this.speedY=-0.3+Math.random()*0.6;
    this.angle=-Math.PI/6;
    this.rotationSpeed=(Math.random()*0.02)-0.01;
    this.color=`hsl(${Math.random()*360},100%,65%)`;
  }
  update(){
    this.x+=this.speedX; this.y+=this.speedY; this.angle+=this.rotationSpeed;
    if(this.x<0||this.x>canvas.width||this.y<0||this.y>canvas.height)this.reset();
    if(mouse.x && mouse.y){
      const dx=this.x-mouse.x; const dy=this.y-mouse.y;
      const dist=Math.sqrt(dx*dx+dy*dy);
      if(dist<120){this.x+=dx/10; this.y+=dy/10;}
    }
  }
  draw(){drawHexagon(this.x,this.y,this.radius,this.angle,this.color);}
}

class Blob{
  constructor(){this.reset();}
  reset(){
    this.x=Math.random()*canvas.width;
    this.y=Math.random()*canvas.height;
    this.radius=60+Math.random()*100;
    this.speedX=-0.1+Math.random()*0.2;
    this.speedY=-0.1+Math.random()*0.2;
    this.color=`hsla(${Math.random()*360},100%,65%,0.15)`;
  }
  update(){ this.x+=this.speedX; this.y+=this.speedY;
    if(this.x<-this.radius||this.x>canvas.width+this.radius||this.y<-this.radius||this.y>canvas.height+this.radius)this.reset();
  }
  draw(){
    const grad=ctx.createRadialGradient(this.x,this.y,0,this.x,this.y,this.radius);
    grad.addColorStop(0,this.color);
    grad.addColorStop(1,'rgba(0,0,0,0)');
    ctx.fillStyle=grad;
    ctx.beginPath();
    ctx.arc(this.x,this.y,this.radius,0,Math.PI*2);
    ctx.fill();
  }
}

for(let i=0;i<particleCount;i++) particles.push(new Particle());
for(let i=0;i<blobCount;i++) blobs.push(new Blob());

function connectParticles(){
  for(let a=0;a<particles.length;a++){
    for(let b=a;b<particles.length;b++){
      const dx=particles[a].x-particles[b].x;
      const dy=particles[a].y-particles[b].y;
      const dist=Math.sqrt(dx*dx+dy*dy);
      if(dist<100){
        ctx.beginPath();
        ctx.strokeStyle = particles[a].color.replace('hsl','hsla').replace(')',',0.5)');
        ctx.lineWidth=1;
        ctx.moveTo(particles[a].x,particles[a].y);
        ctx.lineTo(particles[b].x,particles[b].y);
        ctx.stroke();
      }
    }
  }
}

function animate(){
  ctx.clearRect(0,0,canvas.width,canvas.height);
  blobs.forEach(b=>{b.update();b.draw();});
  particles.forEach(p=>{p.update();p.draw();});
  connectParticles();
  requestAnimationFrame(animate);
}
animate();
</script>
